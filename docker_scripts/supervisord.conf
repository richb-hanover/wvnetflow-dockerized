# supervisord file for webview netflow reporter
[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisord.log
loglevel=debug

[program:rsyslogd]
command=/etc/init.d/rsyslog start
user=root
redirect_stderr=true
stdout_logfile=/dev/stdout
stderr_logfile=/dev/stderr

[program:flowd]
command=/etc/init.d/flowd start
user=root
redirect_stderr=true
stdout_logfile=/dev/stdout
stderr_logfile=/dev/stderr

[program:cron]
command=cron -f -L 15
user=root
redirect_stderr=true
stdout_logfile=/dev/stdout

# [program:apache]
# command=sudo /usr/sbin/apachectl -D FOREGROUND